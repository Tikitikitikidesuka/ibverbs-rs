[package]
name = "pcie40-rs"
version = "0.1.0"
edition = "2024"
description = "Rust bindings for the PCIe40 drivers"

[dependencies]
libc = "0.2.170"
thiserror = "2.0.12"

[build-dependencies]
bindgen = "0.71.1"
