[package]
name = "multi-fragment-packet"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "2.0.12"
pcie40 = { path = "../pcie40", optional = true }
alignment-utils = { path = "../alignment-utils" }
circular-buffer = { path = "../circular-buffer" }

[features]
pcie40-readable = ["dep:pcie40"]
#shmem-element = ["dep:shared_memory_buffer"]
