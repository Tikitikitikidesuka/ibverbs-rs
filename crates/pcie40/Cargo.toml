[package]
name = "pcie40"
version = "0.1.0"
edition = "2024"
description = "Safe Rust PCIe40 bindings wrapper for the LHCb Event Builder"

[dependencies]
thiserror = "2.0.12"
tracing = "0.1.41"
alignment-utils = { path = "../alignment-utils" }
circular-buffer = { path = "../circular-buffer" }

[build-dependencies]
bindgen = { version = "0.72.0" }
